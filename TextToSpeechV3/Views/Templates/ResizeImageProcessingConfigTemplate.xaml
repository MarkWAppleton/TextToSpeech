<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<DataTemplate x:Key="ResizeImageProcessingConfigTemplate">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="auto"/>
				<RowDefinition Height="auto"/>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<Label Grid.Row="0" Content="Resize" FontWeight="Bold"/>
			<Label Grid.Row="1" Content="Might not look different during config"/>

			<Grid Grid.Row="2">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="auto"/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition/>
				</Grid.RowDefinitions>

				<Label Grid.Column="0" Grid.Row="0" Content="Width Multiplier"/>
				<TextBox x:Name="txtWidth" Grid.Column="1" Grid.Row="0" Text="{Binding ImageProcessingConfig.WidthMultiplier}" VerticalAlignment="Center"/>
				<Label Grid.Column="0" Grid.Row="1" Content="Height Multiplier"/>
				<TextBox x:Name="txtHeight" Grid.Column="1" Grid.Row="1" Text="{Binding ImageProcessingConfig.HeightMultiplier}" VerticalAlignment="Center"/>

			</Grid>
		</Grid>
	</DataTemplate>
</ResourceDictionary>
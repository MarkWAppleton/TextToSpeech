<Window x:Class="TextToSpeechV3.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextToSpeechV3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<StackPanel>
		<!--<TextBox Name="textBox" Text="{Binding SelectedText, Mode=TwoWay}"/>
		<Button Content="Button" Command="{Binding Path=SpeechTestButtonCommand}" CommandParameter="{Binding ElementName=textBox,Path=Text}" />
		<TextBox x:Name="txtSpeechRate" Text="{Binding Settings.Rate, Mode=TwoWay}"/>
		<ListBox x:Name="lbVoices" ItemsSource="{Binding Voices}" SelectedItem="{Binding Settings.Voice}"></ListBox>
		<TextBox x:Name="txtResult" IsReadOnly="True"/>
		<TextBlock x:Name="txtCopyResults"/>-->
		<Button Content="Settings" Command="{Binding Path=SettingsButtonCommand}"/>
		<Button Content="Snipping" Command="{Binding Path=SnippingButtonCommand}"/>
	</StackPanel>
</Window>
